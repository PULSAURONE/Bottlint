# Файл .gitignore

# Папки IDE (Pycharm, VSCode и т.д.)
.idea/
.vscode/

# Файлы окружения Python
.env
venv/
.venv/
env/
ENV/
env.bak
venv.bak

# Системные файлы Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Папка с данными, которые не должны быть в репозитории
# Включает скачанные модели, загруженные файлы, токены, логи
/data/

# Если вы хотите, чтобы структура папки data была, но без содержимого
# то вместо /data/ добавьте:
# /data/*
# !/data/.gitkeep
# (И создайте пустой файл .gitkeep в папке data)
# Но для нашего случая проще полностью игнорировать /data/

# Локальные настройки и кеш
*.local
.DS_Store
Thumbs.db